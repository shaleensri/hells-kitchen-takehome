# Base URL the browser uses to reach the backend API.
# Defaults to http://localhost:8080 in lib/api.ts if unset — fine for local
# dev, no setup needed. Set this explicitly for a deployed environment
# (Iteration 8 — e.g. the Render/Railway backend URL when deploying to Vercel).
NEXT_PUBLIC_API_URL=http://localhost:8080
